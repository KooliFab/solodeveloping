// Promo Banner Configuration
// Set isActive to true to show the banner, false to hide it

export const promoBannerConfig = {
  isActive: false, // Change this to false to deactivate the banner
  text: '🎉 Special Offer: Get 20% off on all products! Use code PROMO20 at checkout 🎉',
  scrollSpeed: 20, // Animation duration in seconds (lower = faster)
  backgroundColor: 'from-purple-600 to-purple-700', // Tailwind gradient classes
};
